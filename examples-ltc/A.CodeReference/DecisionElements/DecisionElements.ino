// Love to Code

// This contains reference code to help remind how to write decision
// elements. The code here won't compile and run on its own.

////////// if
if( i < 4 ) {
  // runs this code only if 4 is less than 4
}

////////// if-else
if( x >= 50 ) {
  // runs this code only if x is 50 or larger
 } else {
  // otherwise, runs this code
 }


////////// if-else if-else
if( a < 4 ) {
  // runs this code only if a is less than 4
 } else if( a == 5 ) {
  // run this code only if a is exactly 5
 } else {
  // this code will run in all other cases (a is greater than 5)
 }

// more to come!
